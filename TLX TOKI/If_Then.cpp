#include <stdio.h>

int main()
{
    long long int a;
    scanf("%lld", &a);

    // if (a > 0) printf("%lld\n", a);
    // else {
    // }

    // if (a > 0 && a % 2 == 0) printf("%lld\n", a);
    // else {
    // }

    // if (a > 0) {
    //     printf("positif\n");
    // } else if (a < 0) {
    //     printf("negatif\n");
    // } else {
    //     printf("nol\n");
    // }
}